# 🚀 Raspberry Pi Remote Embedded Control System

[cite_start]본 프로젝트는 **동적 라이브러리(Dynamic Library) 로딩** 방식과 **멀티스레딩**을 활용하여 라즈베리 파이의 하드웨어(LED, 부저, FND, 조도 센서)를 원격으로 제어하는 서버-클라이언트 시스템입니다. [cite: 1]

---

## 📂 프로젝트 구조

| 경로 | 파일명 | 설명 |
|:---|:---|:---|
| `server/` | `server.c` | 백그라운드에서 동작하며 `.so` 라이브러리를 동적으로 로드하고 클라이언트 명령을 처리하는 메인 서버 |
| `client/` | `client.c` | TCP 소켓을 통해 서버에 제어 패킷을 전송하고 센서 상태를 모니터링하는 클라이언트 |
| `libs/` | `led.c`, `buzzer.c`, `fnd.c`, `sensor.c` | [cite_start]하드웨어 제어 로직이 담긴 독립된 동적 라이브러리 소스 [cite: 1] |
| `include/` | `device_interface.h` | 공통 인터페이스, 장치 ID, 핀 번호 정의 헤더 |
| `/` | `Makefile` | [cite_start]서버, 클라이언트 및 `.so` 라이브러리 빌드 자동화 스크립트  |

---

## ✨ 핵심 기술 스택

### 1. 동적 라이브러리 (Dynamic Loading)
* `dlopen`, `dlsym`을 사용하여 실행 중에 하드웨어 제어 모듈을 로드합니다.
* [cite_start]장치별 로직이 독립되어 있어 유지보수가 용이하며 전체 시스템 재컴파일 없이 모듈 업데이트가 가능합니다. [cite: 1, 2]

### 2. 데몬 프로세스 (Daemon Process)
* `fork()`와 `setsid()`를 통해 터미널 제어권을 분리하고 백그라운드에서 상주하며 서비스를 제공합니다.

### 3. 멀티스레드 및 동기화 (POSIX Threads & Mutex)
* **센서 스레드**: 조도 센서를 상시 감시하여 자동 모드 시 LED를 제어합니다.
* **부저 스레드**: 멜로디 연주 중에도 중단 명령을 즉시 처리할 수 있도록 설계되었습니다.
* **뮤텍스(Mutex)**: 여러 스레드가 동시에 GPIO에 접근할 때 데이터 경합 및 충돌을 방지합니다.

---

## 🛠 하드웨어 핀 맵 (WiringPi 기준)



* **LED**: Red(26), Yellow(27), Green(28)
* **Buzzer**: 23 (SoftTone 사용)
* [cite_start]**Sensor**: 24 (Digital Input) [cite: 1]
* **FND (7-Segment)**: A(3), B(0), C(1), D(2) - BCD Decoder 연결

---

## 💻 빌드 및 실행 방법

### 1. 컴파일
프로젝트 루트 디렉토리에서 아래 명령을 입력합니다.
```bash
make
